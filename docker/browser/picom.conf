# Use the GLX backend for better performance and to utilize GPU acceleration
backend = "glx";

# Enable vertical synchronization (VSync) to prevent screen tearing
vsync = true;

# The following options are for performance tuning and are optional
# Uncomment and adjust them as necessary based on your system and needs
# glx-no-stencil = true;
# glx-no-rebind-pixmap = true;
